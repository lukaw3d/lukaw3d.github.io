# Make ESLint in VSCode less annoying

```json
"eslint.rules.customizations": [{ "rule": "*", "severity": "info" }], // Make ESLint errors less annoying
"xo.overrideSeverity": "info", // Make XO lint errors less annoying
```
